{
  "name": "restaurant-analytics",
  "version": "1.0.0",
  "description": "餐饮经营年度数据分析与AI战略建议平台",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "dev": "cd client && npm start",
    "server": "cd server && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
